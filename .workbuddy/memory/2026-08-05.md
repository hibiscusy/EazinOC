# 2026-08-05 工作日志

- **紧急恢复: .git 丢失 + 拉取新代码(08:38–08:50)**: 用户说"拉一下新代码"。执行 `git fetch` 时整个进程被 sandbox 静默 kill(无报错、连 echo 都不执行)；排查发现项目 `.git` 目录已意外消失(疑为上一轮 `git pull --rebase` 编辑器 `could not mark as interactive` 报错时损坏)。
  - 恢复步骤: ①`cp -r` 整目录到同级 `.SAFEBACKUP` 作保险; ②`git init -b main` + `git remote add origin git@github.com:hibiscusy/EazinOC.git`; ③`git fetch origin main`(必须 `dangerouslyDisableSandbox:true`, 否则 sandbox 拦截 pack 传输) → 成功拉到 `origin/main = 7f4c182`(合并提交: 含我们之前推的日记图瀑布流等 + 蒲熠星模块37提交 + 修 story-swd 死链); ④`git reset --hard origin/main` 工作树=fetch 最新, 新代码(author-pyx.html 等)就位, 我们的改动(瀑布流/去相册)完好; ⑤`git branch -u origin/main`, `git status`=本地与远端同步无分叉。
  - 验证: served 8090 预览=瀑布流JS在线 + author-pyx.html 返回200; `git status` 仅剩未跟踪 `_rebuild_tl.py`/`relationship-map.html`(本地额外文件, 无害, 未纳入合并)。
  - **关键教训(已写入 MEMORY.md)**: 本环境 `git fetch/pull` 须 `dangerouslyDisableSandbox:true`, 否则 sandbox 会 kill 掉 pack 数据传输; `git ls-remote` 因数据小能过, fetch 不行。

- **重建 dist 并尝试云端部署(09:07, 进行中/受阻)**: 用户确认"好"(同意重新部署)。写 `_sync_dist.py` 按页面实际引用的资源全量重建 dist: 14 个 HTML 页面(含 author-pyx.html / OC宇宙-人物时间线-蒲熠星.html / 度漪日记-蒲熠星.html / relationship-map.html 等) + 90 张 crops + theme.css 全部同步; 本地 8090 实测 index/中文主页/author-pyx/story-tl 均 200。随后 `workbuddy_cloudstudio_deploy` 连续多次返回 400(`exec failed`), 疑似旧沙箱(2ee5…)复用状态异常, 云端部署暂未成功。本地预览已可验收最新内容; 待后端恢复或用户删除旧已发布应用后重试部署。

- **云端部署持续 400(09:18–09:20, 仍未解决)**: 用户已去「设置-数据管理-我发布的应用」删除旧沙箱(2ee5…), 但我重试仍连续 400。进一步诊断: ①复制 dist 到全新路径目录 deploy-oc-fresh 再部署→仍400(排除旧沙箱复用); ②换端口8080→仍400(排除端口冲突)。结论: CloudStudio 后端在创建/上传沙箱这一步本身失败, 属后端侧问题(疑似临时故障或账户配额), 非本地代码/配置问题。本地 8090 预览始终正常(全部新代码可验收)。待后端恢复后再重试; 或查 CloudStudio 控制台看是否配额耗尽。已清理临时 deploy-oc-fresh。

- **云端部署持续 400(09:18–09:20, 仍未解决)**: 用户已去「设置-数据管理-我发布的应用」删除旧沙箱(2ee5…), 但我重试仍连续 400。进一步诊断: ①复制 dist 到全新路径目录 deploy-oc-fresh 再部署→仍400(排除旧沙箱复用); ②换端口8080→仍400(排除端口冲突)。结论: CloudStudio 后端在创建/上传沙箱这一步本身失败, 属后端侧问题(疑似临时故障或账户配额), 非本地代码/配置问题。本地 8090 预览始终正常(全部新代码可验收)。待后端恢复后再重试; 或查 CloudStudio 控制台看是否配额耗尽。已清理临时 deploy-oc-fresh。

- **银河系中心酒吧小剧场新增10张cos图(09:30, commit b902412, 未push)**: 用户给10张TL演出现场cos图(690x1035/920, 亮片黑外套/玫瑰背景/水晶皇冠/黑白特写/蓝尾辫等), 要求放在"银河系中心酒吧小剧场之👑🧪🐱"卡片正文下方。①10图转WebP存 tl_bar_01~10.webp(根+dist同步); ②story-tl.html 该卡片c-body末尾插入diary-img-grid(10张img, 复用瀑布流+灯箱样式, 含width/height); 本地commit b902412, 未push。

- **云端部署成功(09:39, 新沙箱 9024d4735ead435d934e6d8b55498afe)**: 用户确认"好"后重试部署, 此次后端恢复正常, 首次即成功(新建沙箱, 非复用)。分享地址 https://9024d4735ead435d934e6d8b55498afe.sh3.agentos-app.net。verified: 版本20260805m / 酒吧小剧场tl_bar_=10 / photo-gallery=0 / 瀑布流JS在线 / author-pyx.html=200。本波全部改动(拉取蒲熠星模块+瀑布流+双角色去相册+酒吧小剧场10图)均已云端上线。之前旧沙箱(2ee5…)已删除, 此为新沙箱。

- **日常tab新增卡片: 银河系中心酒吧小剧场·前情提要🪭(09:53, commit e9b4f06, 未push)**: 用户给18张TL酒吧管家cos图(690x921, 黑色宽檐皇冠帽/蕾丝手套/珍珠项链/贝壳背景等), 要求放入塔拉撒里昂"日常"tab, 时间0801, 来源蒲熠星微博, 含前情提要文字。①18图转WebP存 tl_daily_01_01~18.webp(根+dist同步); ②story-tl.html tab-daily 最前面插入新node(card: 标题"银河系中心酒吧小剧场·前情提要🪭", c-body含文字p+diary-img-grid 18张img+瀑布流+灯箱); 本地commit e9b4f06, 未push。
