# 2026-08-13 工作日志

## 烬行故事页（story-jx.html）新增内容卡片
- 「蕨丛里的铃兰」（过去·诗，发布 2026.05.17）：8 行诗 + 16 图 `crops/jx_lantern_01~16.webp`
- 「家务机器人」（现在·日常，发布 2026.05.16）：烬行/斯沃德对话 + 18 图 `crops/jx_robot_01~18.webp`
- 「擦杯子最厉害」（现在·日常，发布 2026.05.16）：斯沃德/谶对话 + 17 图 `crops/jx_cup_01~17.webp`
- 顺序：2026.05.17（蕨丛、语音十二条）→ 2026.05.16（家务机器人、擦杯子最厉害、房租账单）→ 童年（大侠披风）。同日倒序置后。
- 图源：剪贴板图片（时间戳 03-43 / 04-04 / 04-10），PIL 转 webp q85。

## 注意
- Git Bash 下 Windows 路径反斜杠会被转义，python 命令须用正斜杠路径（如 `C:/Users/.../python.exe`）。
- 之前多轮「请继续完成」报 400 是图片转换命令路径转义失败所致，已用正斜杠修复。

## 永久站点上线（GitHub Pages / gh-pages 分支）
- 用户发现 `https://hibiscusy.github.io/EazinOC/` 没更新。根因：GitHub Pages 源= `gh-pages` 分支，上次只推了 `main`（含 CloudStudio 部署），永久站点停在旧版 `066e75d`。
- 用 worktree 建干净 `gh-pages`（仅站点文件 + `.nojekyll`，不含 `.workbuddy`/脚本/源 jpg），强制推送 → `8bc3c69`。已 WebFetch 验证线上 `story-jx.html` 七张卡片全部显示。
- 后续建议：把 Pages Source 改为 `main` 分支即可「推=自动上线」，省去每次同步 gh-pages。详见 MEMORY.md 新增约定。
