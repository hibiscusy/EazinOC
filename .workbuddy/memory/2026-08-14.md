# 2026-08-14 工作日志

## 烬行内页 story-jx.html
- 新增诗卡「放走的绿意」（过去·诗，2026.05.15），含 2 张图 crops/jx_fern_01~02.webp。时间线共 8 张。

## 谶内页 story-chen.html
- 新增日常卡「风与浪 · 养宠物」（2026.05.01），含两段对话（塔拉撒里昂/度漪）+ 11 张图 crops/chen_daily_01~11.webp。
- **删除 Tab 切换结构**（原"谶的诞生"/"日常"两个 tab），重写为单一 `<section class="line">` 内含单个 `.tl`，9 篇全部按时间倒序（最新在前）重排：
  2026.06.23《走进古地球》→ 2026.05.01 你最近为什么总跟着我 → 2026.05.01 风与浪·养宠物 → 2026.04.30 掉头发 → 2026.04.19 诞生(一)(二)(三) → 2025.10 剑心成 → 2025.10.06 你叫他什么师父。
- 注意：删除 tab 后已移除 `tabs.js` 引用（页面仅保留回到顶部脚本）。

## 注意
- Git Bash 下 Windows 路径反斜杠会被转义，python 图片转换命令须用正斜杠路径。
- 剪贴板图片位于 C:/Users/admin/.workbuddy/clipboard-images/，转 webp 用 quality=85。
- **packed-refs 修复**：git push 后本地 `git status` 仍报 ahead/behind 是因为 `.git/packed-refs` 中 `refs/remotes/origin/main` 锁住了旧 SHA，`update-ref` 无法修改。需用 `sed -i` 直接替换 packed-refs 那行。修正后状态立刻干净。

## 一次性推+上线（2026-08-14 下午）
- 谶内页「你最近为什么总跟着我」补 15 张图（crops/chen_follow_01~15.webp），第 1/8 张 690×460 横版。
- 谶内页「你能治好掉头发吗」正文升级（每句 `.q` 高亮 + 新增结尾"化毛膏？应该是武林秘药吧？"），新增 18 张图（crops/chen_hair_01~18.webp，第 4/8/12 张横版）。
- 一次性推+上线：本地 commit `e62bdac` → push origin main → 部署 CloudStudio 沙箱 → 同步 gh-pages `8bc3c69 → e94c454`，全部成功。本地 main = origin/main = `e62bdac`。

## 图片加载速度优化（2026-08-14 16:27 任务）
- **现状诊断**：565 张 webp，共 17.6MB（主题图在前）；所有页面 `loading="lazy" decoding="async"` 已基本覆盖；hero/首屏图已 `fetchpriority="high"`；主页面角色卡已 `srcset` 响应式 1600w/800w。
- **3 项优化已落地**：
  1. **批量重转 webp** quality 85→72（PIL + 系统 tmp + shutil.move），实际节省 ~2%（部分图原本已较低质量），但统一了质量标准。
  2. **CSS 加 `content-visibility: auto`** 到 `.card`（含 `contain-intrinsic-size: auto 420px`）—— 这是最大收益项，首次渲染只 paint 视口内卡片，滚动时按需渲染；Chrome 85+/Safari 18+ 支持，旧浏览器自动降级。
  3. **补全 `decoding="async"`**：story-chen.html 44 张 + story-jx.html 2 张（本次新增的图遗漏了），已用 Python 批量补上。
- **theme.css 版本号**：所有 11 个 HTML 的 `theme.css?v=20260806c` → `?v=20260814a`，避免浏览器缓存旧 CSS。
- **经验**：PIL 在 Windows 不能 `read + save` 同一文件，必须用临时文件（写到系统 tmpdir 避免 sandbox 拦截）。
- **未来若再加更多图**：建议新增时直接用 quality=72、`<img loading="lazy" decoding="async" />`，省得再次批量改。
