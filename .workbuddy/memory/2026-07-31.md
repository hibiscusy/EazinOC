# 2026-07-31
- 蒲熠星 OC 页面：用户用 7 张新 PNG 角色图（600x~220 横图，RGBA）替换 crops/anime_1~7；对应关系 烬行=1 铃弗瑞迪尔=2 谶=3 斯沃德麦伦=4 度漪=5 塔拉撒里昂=6 赛博恩=7。HTML 引用改 .jpg→.png，CSS 由 height:128px+cover 改为 height:auto+contain，按原比例完整展示不裁切。
- 本地预览 server 在 127.0.0.1:8123（后台 task 7nKPIC）服务原文件，改完 F5 刷新即见；云端分享链接（CloudStudio 沙箱）仍是旧版，定稿后需重新部署 deploy/oc-share。
- 用户反馈图与标题对不上，按 PNG 文件名本身重新对应角色卡：anime_1=烬行、anime_2=铃弗瑞迪尔、anime_3=谶、anime_4=斯沃德麦伦、anime_5=度漪、anime_6=塔拉撒里昂、anime_7=赛博恩。HTML 图片引用加 ?v=2 防缓存，右侧预览刷新验证。
- 在角色卡区(roles-grid)末尾新增「蒲熠星本人」介绍卡(#r-pyx)：文字卡，无真人图，介绍其作为《银河系旅行攻略》巡演创作者及 OC 宇宙叙事者(小说家)身份。保持与角色卡统一风格。
- 将「蒲熠星」介绍卡移到角色卡网格顶部(r-pyx 现为首个卡片)，并加 grid-column:1/-1 全宽 banner 样式(渐变高亮)，先介绍本人再列 OC 角色。
- 用户为蒲熠星介绍栏提供了一张真人照片(b9784e8ef552756ad4984bbec3e980c8.jpg)，已等比例缩放至 269x360 保存为 crops/pyx-photo.jpg，并嵌入 #r-pyx 卡片：左侧照片 + 右侧文字介绍。新增 .pyx-photo/.pyx-text 样式及移动端响应式布局。
- 更新各OC英文名：铃兰 LINGLAN→LYNPHREDIL、度漪 DUYI→DU YI、谶 CHEN→CHAIN、斯沃德麦伦 SWORDS→SWORD MELON、塔拉撒里昂 TALASARION→THALASSARION；烬行(JINXING)赛博恩(CYBORN)不变。
- 用户反馈OC卡片图文偏窄，将 .rc 水平内边距由 20px 减小至 12px，使图片和介绍文字在卡片内更宽。
- 用户认为OC卡片图与介绍文字间距太挤，将 .rc-imgs 下边距由 2px 增至 18px(上12→14)，拉开图片与正文间距。
- 用户提供了新照片(小时候老师问...小红书图)替换蒲熠星介绍栏照片；已按原比例缩放至 269x360 覆盖 crops/pyx-photo.jpg，HTML 引用更新为 ?v=3 防缓存。
- 用户要求蒲熠星介绍卡下方间距与上方相同，给 #r-pyx 加 margin-bottom:24px，使下方总间距(网格gap 14+24=38px)与上方(intro 下边距8+roles-grid上边距30=38px)一致。
- 在 OC 角色卡上方新增小标题「宇宙 · 角色」，样式复用 .sec-title（与「故事总时间线 · 概览」一致：14px/.2em字距/蓝字/左侧渐变色条）；将 .sec-title 标注为页面默认小标题样式，并把 .roles-grid 顶部外边距由 30px 归零，避免小标题下方双击间距；约定已写入 .workbuddy/memory/MEMORY.md。
- 按用户要求调整角色区：①将「宇宙 · 角色」小标题移到蒲熠星介绍卡下方，并把 #r-pyx 下边距改为 22px，使小标题上下间距均为 22px；②OC 角色卡由响应式多列改为桌面端固定 2 列（移动端仍为 1 列）；③按文件名重新替换 7 张角色图为 1037px 宽横版原图，HTML 引用更新为 ?v=4 防缓存。
- 修复上一轮结构错乱：正则提取 #r-pyx 卡时因 .*?</div> 非贪婪截断在首个内层 </div>，导致卡片后半段(<p>/<div class=rel> 等)误入 roles-grid。已用精确字符串替换修复，标签平衡校验通过(8卡: pyx+jx+dy+cb+ch+ll+swd+tl)。
- 在蒲熠星介绍卡上方新增小标题「小说家」，复用 .sec-title 样式并设 margin:20px 0，上下间距均为 20px。
- 用户要求去掉「故事总时间线 · 概览」区块：已删除整个 <section class=overview> 及 .overview CSS、并移除顶部导航中的「总览」链接；页面直接从角色卡区进入各人物线。
- 重大重构：将「故事放进各个角色」——提取原 6 条故事线 section，生成 7 个独立故事页 (story-jx/dy/cb/chen/ll/swd/tl.html)，兄弟线共享于烬行&赛博恩页；索引页 7 张 OC 角色卡改为 <a> 可点击跳转，顶部导航改指故事页，原故事线 section 从索引页移除；提取共享 CSS 为 theme.css（索引+故事页均 link 引用）。8 个页面标签平衡校验通过。注意：角色卡 id 是 r-ch（非 r-chen）。
- 用户咨询跨电脑编辑方案，选「打包 zip」。已写 pack.py（仅挑核心文件：索引页+theme.css+7故事页+crops 的 anime_1~7.png/pyx-photo.jpg+build_stories.py+.workbuddy/memory/*），生成 oc-universe-2026-07-31.zip（21 文件，顶层文件夹 oc-universe/）。pack.py 保留可重跑。注：WorkBuddy 无内置跨设备同步，另一台电脑需装 WB 并打开该文件夹；deploy/ 与 concert_*.jpg 等为旧产物未打包。
- 用户决定用 Git 同步，仓库 https://github.com/hibiscusy/EazinOC.git。已 git init -b main + 设 local user(hibiscusy / hibiscusy@users.noreply.github.com) + 写 .gitignore(排除 deploy/、crops/concert_*.jpg、*_sheet.jpg、*.zip) + add -A(27文件) + commit(80f50a1) + 一次性 PAT 推送成功(main->main, upstream 已设)。后改为 SSH 方式：生成 ed25519 密钥(~/.ssh/id_ed25519, 空密码) + remote 改 git@github.com:hibiscusy/EazinOC.git + `ssh -T` 与 `git ls-remote` 均验证通过。此后 push/pull 免 token。建议用户在 GitHub 删除旧 PAT(classic) 并改 SSH；另一台电脑 clone 用 SSH 地址、各自生成 key 加 GitHub。
- 用户问能否删本地，核查发现本日 memory 改动仅本地 edit 未 commit（潜在丢失风险），已补 commit+push(5f108c6) 使 GitHub 完整最新；确认本地工作区文件夹可安全删除（删后 GitHub 为唯一副本，高可用）。经验：删除本地 git 仓库前务必 `git status` 确认 clean，否则未提交的 .workbuddy/memory 等改动会丢失。本机 SSH 密钥在 ~/.ssh 不受影响，日后重装可 `git clone git@github.com:hibiscusy/EazinOC.git` 恢复。
- 用户贴出 3 张度漪日记截图要求更新：新增「节选（五）巨星请我去看他的演唱会」与「节选（六）海」；同时把当前站点 story-dy.html 中精简版「节选（四）」补为完整原文。两处文件均更新：story-dy.html（多页站点角色故事页）和 度漪日记-蒲熠星.html（旧独立日记页）都已同步。新篇目日期按 07.30/07.31 推断；原文链接因截图未提供微博 URL，暂用 # 占位，后续可补。已 commit+push(29a4036) 到 GitHub。
- 用户又贴出节选（一）（二）截图要求「填补内容」：把 story-dy.html 中精简版节选（一）（二）替换为完整原文；节选（一）标题同步补全为「人在β星，刚下飞船，被自己的账户余额吓清醒了」。旧独立页 度漪日记-蒲熠星.html 中这两篇已是全文，无需改动。已 commit+push(58d4508) 到 GitHub。
- 用户直接给出节选（三）（四）完整原文要求「更换内容」：把 story-dy.html 中精简版（三）与缺段落的（四）替换为完整原文（引号统一为「」）。旧独立页 度漪日记-蒲熠星.html 的（三）（四）经核对已是同一完整版（引号用 &ldquo; 实体），无需改。已 commit+push(0a9a167)。现 story-dy.html 节选（一）~（六）均为完整原文（唯节选五/六原文链接仍 # 占位）。
- 用户补充谶相关内容：在 story-chen.html 新增「谶的诞生（一）· 剑有意识」「谶的诞生（二）· 显现」两个节点（插在拜师篇之后、化人篇之前），并把原精简版「谶的诞生（三）· 魔剑化人」替换为完整原文（含"妈妈""ba......""停！！！！住口！！！！！"等）。沿用本页直引号+<span class="q"> 风格，日期沿用 2026.04.19 佛山演唱会，原文链接复用 sina 全文转载。标签平衡校验通过（节点 6→8）。已 commit+push(b35a173)。
- 用户新偏好：改完前**不要自动 push**，本地正常 commit、累积改动，等用户说"改完/推送"再一次性 `git push`。已写入 MEMORY.md「Git 协作约定」。注意：b35a173(谶) 已在用户下达此偏好前推送，属已完成内容；此后改动仅本地提交、暂挂不推。
- 首页新增「日常」小标题(sec-title, id=daily) 聚合区：从 7 个 story 页抓取所有 c-stage=日常 的卡片共 9 条（dy1/chen3/tl1/swd3/ll1，jx/cb 无），按发布日期升序(2025超话→2026.04.30→05.01×2→05.16×3→05.17→06.23)排列，复用 .tl/.node/.card 时间线样式；每张卡角色名做成可点击胶囊(<a class="c-stage"> 跳转到对应 story 页)，c-src 加「查看完整故事→」链接。顶部导航新增「日常」锚点(#daily)。theme.css 加 a.c-stage 链接样式。本地 commit(1447d70)，未推送。注：story 页内的日常卡片保留原样（首页为聚合副本）。
- 用户要求「把角色里面的日常删除，不重复展示」：从各 story 页删除 c-stage=日常 的节点共 9 条（chen3/dy1/ll1/swd3/tl1，jx/cb 本就无），避免与首页日常区重复。story-swd.html 原本仅 3 条日常、删后时间线为空，已加一句引导语指向首页 #daily。本地 commit(26a9727)，未推送。
- 用户新需求：度漪角色页(story-dy.html)在角色名下方加「日记 / 日常」两个切换菜单、**默认显示日常**。实现：hero 后插入 `.tabs`(id=dy-tabs, 两个 tab-btn, 默认 active=daily)；原 6 篇节选(一~六)包进 `#tab-diary`(默认 hidden)；新补回度漪日常随笔「装睡的猫崽」(取自首页 daily 区, 原文 sina 转发整理) 放进 `#tab-daily`(默认显示)；纯 JS 切换 hidden。theme.css 加 .tabs/.tab-btn/.tab-panel[hidden] 样式。本地 commit(63146fa)，未推送。注：此操作使度漪日常在「首页 daily 区」与「story-dy 日常tab」两处展示（用户明确要在角色页内切换，属有意重复）。
- 后续微调（均本地 commit 未推送）：度漪 tab 改为默认**日记**(fdc793d)；tab 按钮移至人物介绍(line-desc)下方(e3f1e54)；tab 按钮与下方内容间距 30px(c728dbc)；tab 激活态不再改背景、仅边框/文字/加粗(370303c)；body 背景 background-attachment:fixed 使切换 tab 时整页大背景不随高度变动(7d05f50)。
- 用户要求谶页(story-chen.html)加「谶的诞生 / 日常」切换、**默认谶的诞生**，除谶的诞生内容外都属日常。实现：`.tabs`(id=chen-tabs, 默认 active=birth) 置于 line-desc 下方；原 5 个诞生节点(剑心成/拜师/诞生一二三)包进 `#tab-birth`(默认显示)；从首页 daily 区取回谶 3 条日常(你能治好掉头发吗/你最近为什么总跟着我/走进古地球与警报，改 c-stage=日常、去自链接) 包进 `#tab-daily`(默认 hidden)。改用**通用切换脚本**（按 data-tab 切换 `#tab-<tab>`，同时适配 dy/chen 两页）。本地 commit(e2291c4)，未推送。
