# 旧部署产物（重新部署会再生成）
deploy/

# 早期素材（当前站点未引用）
crops/concert_*.jpg
crops/*_sheet.jpg

# 打包产物
*.zip

# 构建/部署产物（由 build_deploy.py 生成，重新部署会再生成）
dist/
dist_old/
dist_old*/

# 系统/编辑器
.DS_Store
Thumbs.db

# 一次性临时脚本（非站点文件）
adjust_tl.py
uniform_src.py
make_stars*.py
_t*.webp
nul

# 一次性临时脚本（非站点文件）
check_story_lines.py
daily_break.py
daily_roles_break.py
transform_daily.py
transform_story_lines.py
